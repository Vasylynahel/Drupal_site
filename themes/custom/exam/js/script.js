(function ($, Drupal) {
  Drupal.behaviors.examBehavior = {
    attach: function (context, settings) {
      console.log('Exam theme JS loaded.');
      // Тут можна додати JS логіку.
    }
  };
})(jQuery, Drupal);

