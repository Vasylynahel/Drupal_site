(function ($, Drupal) {
  Drupal.behaviors.examSubBehavior = {
    attach: function (context, settings) {
      console.log('Exam Sub sJS loaded.');
      // Додати специфічний JS для субтеми.
    }
  };
})(jQuery, Drupal);

