# Середовище: dev / stage / prod
APP_ENV=dev

# URL до бази даних (MySQL, PostgreSQL тощо)
# Формат: mysql://USERNAME:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=mysql://drupal:drupal@127.0.0.1:3306/drupal

# Drupal hash salt — можеш згенерувати окремо або залишити порожнім (заповнюється вручну)
# HASH_SALT=some-long-random-string

# (Необов'язково) Email адміністратора
ADMIN_EMAIL=admin@example.com

